# 二级问题：你这个 4 层血缘图结构到底是哪 4 层？为什么这么分？
## 1. 代码位置
- 文件：`src/lineage_graphrag/graph/kt_builder.py`
- 类 / 函数：`LineageKTBuilder.build`
- 关键代码：
```python
graph.add_node(attr_id, label="attribute", level=1, ...)
graph.add_node(entity_id, label="entity", level=2, ...)
self.community_builder.build(graph)
```
- 文件：`src/lineage_graphrag/graph/community_builder.py`
- 类 / 函数：`CommunityBuilder.build`
- 关键代码：
```python
graph.add_node(keyword_id, label="keyword", level=3, ...)
graph.add_node(community_id, label="community", level=4, ...)
graph.add_edge(member, community_id, relation="member_of", ...)
```
## 2. 它在主链路中的位置
它在导入建图阶段。前面是 `/v1/graphs/import` 接口完成解析和标准化，后面是 `GraphRepository.save_graph`、`SnapshotStore.save` 和 FalkorDB 镜像持久化。
## 3. 这段代码做了什么
按照执行顺序，`LineageKTBuilder.build` 先创建 `nx.MultiDiGraph`，再通过 `EvidenceBuilder` 生成证据 chunks。然后它把业务实体建成 level 2 的 `entity` 节点，把实体属性和 description 拆成 level 1 的 `attribute` 节点，并通过 `has_attribute` 边连回实体。接着把标准化后的 `has_relations` 写成实体之间的 `has` 边，把 `transitions` 写成实体之间的 `transitions` 边。最后调用 `CommunityBuilder.build`，在实体拓扑上做社区检测，生成 level 4 的 `community` 节点，再为社区代表实体生成 level 3 的 `keyword` 节点。
## 4. 输入和输出
### 输入
`NormalizedLineage`，里面包含 `entities`、`transitions`、`has_relations`。
### 输出
`GraphBuildResult`，包含 `graph`、`evidence_chunks`、`metadata`。其中 `metadata` 会统计 entity、attribute、keyword、community 和边数量。
## 5. 它有什么用
4 层结构把不同粒度的信息分开了：属性层解决字段、类型、描述等细粒度语义；实体层表示真实业务对象；关键词层表示社区代表节点，方便摘要和召回；社区层把局部子图聚合成更高层语义单元。这样做的价值是既能做精确血缘链路查询，也能支持自然语言问题的粗粒度召回。如果所有信息都塞进实体属性里，RAG 检索会更依赖文本相似度，多跳依赖和社区范围问题就更难命中。
## 6. 调用链路
```text
/v1/graphs/import
→ import_lineage()
→ _materialize_graph()
→ LineageKTBuilder.build()
→ EvidenceBuilder.build()
→ CommunityBuilder.build()
→ GraphRepository.save_graph()
→ SnapshotStore.save()
```
## 7. 面试表达版本
我这个项目里的 4 层不是抽象概念，而是在建图代码里真的落成了不同 label 和 level。level 1 是 attribute，把实体属性和描述拆出来；level 2 是 entity，表示字段、表、指标、服务这些业务节点；level 3 是 keyword，其实是社区里的代表实体；level 4 是 community，用社区检测把局部血缘子图聚合起来。这样设计的好处是，一方面保留细粒度依赖，能做准确链路分析；另一方面给 RAG 一个更高层的摘要入口，复杂问题不一定只能靠单个节点命中。

# 二级问题：导入一个 lineage JSON 后，完整建图链路怎么走？
## 1. 代码位置
- 文件：`src/lineage_graphrag/api/routes_ingest.py`
- 类 / 函数：`import_lineage`、`_materialize_graph`
- 关键代码：
```python
lineage = parser.parse(payload.lineage_json)
normalized = normalizer.normalize(lineage)
repo.save_lineage(payload.graph_id, normalized)
build_result = _materialize_graph(payload.graph_id, request, repo)
```
```python
result = builder.build(normalized)
mirror_status = repo.save_graph(graph_id, result.graph, result.evidence_chunks, result.metadata)
snapshot_store.save(graph_id, result.graph, result.evidence_chunks)
```
- 文件：`src/lineage_graphrag/ingest/parser.py`
- 类 / 函数：`LineageParser.parse`
- 文件：`src/lineage_graphrag/ingest/normalizer.py`
- 类 / 函数：`LineageNormalizer.normalize`
- 文件：`src/lineage_graphrag/graph/kt_builder.py`
- 类 / 函数：`LineageKTBuilder.build`
## 2. 它在主链路中的位置
这是项目的第一条主链路。前面是客户端提交 `graph_id` 和 `lineage_json`，后面是查询接口 `/v1/queries/ask` 和影响分析接口 `/v1/impact/what-if` 基于构建好的图工作。
## 3. 这段代码做了什么
接口收到 `ImportRequest` 后，先用 `LineageParser` 把输入转成 canonical 格式，并校验 entity、children、transition、has relation 的交叉引用。然后 `LineageNormalizer` 把 entity map 排序成 entity list，并在没有显式 `has_relations` 时，把每个 entity 的 `children` 转成 `HasRelation(origin="children")`。之后 `repo.save_lineage` 暂存标准化 lineage，`_materialize_graph` 再调用 `LineageKTBuilder.build` 生成 NetworkX 图和 evidence chunks。最后 `GraphRepository.save_graph` 把图保存到内存并尽力写入 FalkorDB，`SnapshotStore.save` 写本地快照。
## 4. 输入和输出
### 输入
`ImportRequest`：`graph_id` 和 `lineage_json`。
### 输出
接口返回 `graph_id`、`metadata`、`chunks` 数量、`falkordb` 写入状态、`entities` 数量、`transitions` 数量、`has_relations` 数量和 `auto_built=True`。
## 5. 它有什么用
这条链路把不稳定的外部输入变成项目内部稳定的图结构。parser 负责格式兼容和引用校验，normalizer 负责把隐含层级关系显式化，builder 负责把标准数据落到图模型，repository/snapshot/FalkorDB 负责查询时可复用。如果没有这层导入建图链路，后面的 GraphRAG 和 what-if 分析就会直接面对各种不一致输入，稳定性和可维护性都会很差。
## 6. 调用链路
```text
POST /v1/graphs/import
→ import_lineage(payload)
→ LineageParser.parse(payload.lineage_json)
→ LineageNormalizer.normalize(lineage)
→ GraphRepository.save_lineage(graph_id, normalized)
→ _materialize_graph(graph_id)
→ LineageKTBuilder.build(normalized)
→ GraphRepository.save_graph(graph_id, graph, chunks, metadata)
→ SnapshotStore.save(graph_id, graph, chunks)
→ 返回导入和构建结果
```
## 7. 面试表达版本
导入链路我做成了一个比较明确的 pipeline。接口拿到 JSON 后不会直接建图，而是先 parse，把 entities 和 transitions 转成统一格式并做引用校验；再 normalize，把 children 这种隐式层级关系物化成 has relation；然后 builder 把它建成 MultiDiGraph，同时生成 evidence chunks。构建完成后图会进内存，写本地 snapshot，并尽力同步到 FalkorDB。这样后续查询和影响分析只依赖稳定的内部图模型，不需要关心原始数据格式差异。

# 二级问题：为什么使用 `NetworkX.MultiDiGraph`？
## 1. 代码位置
- 文件：`src/lineage_graphrag/graph/kt_builder.py`
- 类 / 函数：`LineageKTBuilder.build`
- 关键代码：
```python
graph = nx.MultiDiGraph()
graph.add_edge(entity_id, attr_id, relation="has_attribute", ...)
graph.add_edge(relation.source, relation.target, relation="has", ...)
graph.add_edge(transition.source, transition.target, relation="transitions", ...)
```
- 文件：`src/lineage_graphrag/retrieval/dual_faiss_retriever.py`
- 类 / 函数：`DualPathFAISSRetriever._path1_node_relation`
- 关键代码：
```python
for u, v, edge_data in graph.out_edges(node_id, data=True):
    key = (u, str(edge_data.get("relation", "")), v)
```
- 文件：`src/lineage_graphrag/impact/propagation.py`
- 类 / 函数：`ImpactAnalyzer._downstream_paths`
## 2. 它在主链路中的位置
`MultiDiGraph` 是建图后的核心数据结构。导入链路生成它，查询链路和影响分析链路都围绕它做检索、遍历和传播。
## 3. 这段代码做了什么
`LineageKTBuilder` 用 `nx.MultiDiGraph()` 创建有向多重图。实体和属性之间是 `has_attribute`，实体和实体之间可能有 `has`，也可能有一条或多条 `transitions`。检索阶段通过 `out_edges` 和 `in_edges` 拿到节点邻边，影响分析阶段通过 `out_edges` 做下游 BFS。
## 4. 输入和输出
### 输入
标准化后的 lineage 数据，包括实体、属性、层级关系和 transition 依赖。
### 输出
一个允许有向边、允许同一对节点之间存在多条不同关系边的图对象。
## 5. 它有什么用
血缘关系天然是有方向的，因为要区分上游和下游；同时同一对节点之间可能既有层级归属，也有数据流转，甚至有多条不同 transition。普通 `DiGraph` 对同一对节点只能保留一条边，容易覆盖关系信息。`MultiDiGraph` 能保留多条边的 relation、relation_properties 和 evidence_refs，对后面的检索、答案追溯和影响分析都更安全。
## 6. 调用链路
```text
LineageKTBuilder.build()
→ nx.MultiDiGraph()
→ add_node(entity/attribute/community/keyword)
→ add_edge(has_attribute/has/transitions/member_of/...)
→ LineageRetriever.retrieve() 读取 in_edges/out_edges
→ ImpactAnalyzer._downstream_paths() 读取 out_edges
```
## 7. 面试表达版本
我用 `MultiDiGraph` 主要是因为血缘图既有方向，又可能有多重关系。比如两个实体之间可能有层级上的 has，也可能有数据流上的 transitions；如果用普通 DiGraph，后写的边可能覆盖先写的边，证据引用也会丢。`MultiDiGraph` 可以把不同 relation 和 evidence_refs 都保留下来，这对检索时返回三元组、what-if 下游传播，以及最终答案可追溯都很关键。

# 二级问题：FalkorDB 在系统里承担什么角色？为什么不是唯一 source of truth？
## 1. 代码位置
- 文件：`src/lineage_graphrag/storage/graph_repository.py`
- 类 / 函数：`GraphRepository`
- 关键代码：
```python
self._graphs[graph_id] = graph
self._chunks[graph_id] = chunks
self._metadata[graph_id] = metadata
if self.falkor is not None:
    mirror_status = self.falkor.write_graph(graph, graph_name=graph_id, chunks=chunks)
```
- 文件：`src/lineage_graphrag/storage/falkordb_client.py`
- 类 / 函数：`FalkorDBClient.write_graph`
- 关键代码：
```python
partitions = _partition_graph_for_falkordb(graph)
raw_name = _raw_graph_name(graph_name)
comm_name = _comm_graph_name(graph_name)
repre_name = _repre_graph_name(graph_name)
```
- 文件：`src/lineage_graphrag/storage/snapshot_store.py`
- 类 / 函数：`SnapshotStore.save`、`SnapshotStore.load`
## 2. 它在主链路中的位置
它位于建图完成后的持久化环节。前面是 `LineageKTBuilder.build` 生成图，后面是服务重启时 `hydrate_startup` 从 snapshot 或 FalkorDB 恢复图。
## 3. 这段代码做了什么
`GraphRepository.save_graph` 先把图、chunks 和 metadata 放进内存字典，这是接口查询时的直接来源。然后如果配置启用了 FalkorDB，就调用 `FalkorDBClient.write_graph` 尝试写入。FalkorDB 写入时会把大图拆成 `graph_raw`、`graph_comm`、`graph_repre` 三类物理图，分别保存原始实体属性关系、社区关系和代表关系。无论 FalkorDB 是否可用，接口都会返回 mirror status。
## 4. 输入和输出
### 输入
`graph_id`、NetworkX 图、evidence chunks、metadata。
### 输出
内存中可查询的图数据，以及一个 FalkorDB 写入状态字典，里面包含 enabled、available、written、error、nodes_attempted、edges_attempted 等信息。
## 5. 它有什么用
这个设计把在线服务的可用性和外部图数据库解耦了。内存图是当前进程的查询 source of truth，snapshot 是本地恢复保障，FalkorDB 是面向持久化、外部图查询和重启恢复的镜像。如果 FalkorDB 抖动，导入接口仍然可以完成，查询链路也不会立刻不可用。它的缺点是多副本一致性需要额外治理，目前代码里是 best-effort mirror。
## 6. 调用链路
```text
LineageKTBuilder.build()
→ GraphRepository.save_graph()
→ 写入 _graphs/_chunks/_metadata
→ FalkorDBClient.write_graph()
→ _partition_graph_for_falkordb()
→ _write_sub_graph(graph_raw/graph_comm/graph_repre)
→ write_chunks()
```
## 7. 面试表达版本
FalkorDB 在我这个项目里更像持久化和外部查询的 mirror，不是在线逻辑唯一依赖的 source of truth。导入成功后图先进入 `GraphRepository` 的内存结构，查询和影响分析直接读内存图；同时写 snapshot，并 best-effort 同步到 FalkorDB。这样做是为了保证可用性，图数据库不可用时不会让整个导入和问答链路失败。FalkorDB 这边我还做了分区写入，把原始图、社区图、代表关系拆开，便于十万级关系下更好管理。

# 二级问题：双路检索具体是哪两路？分别解决什么问题？
## 1. 代码位置
- 文件：`src/lineage_graphrag/retrieval/dual_faiss_retriever.py`
- 类 / 函数：`DualPathFAISSRetriever.retrieve`
- 关键代码：
```python
path1 = self._path1_node_relation(graph, query_vec, top_k)
path2 = self._path2_triple_community(graph, query_vec, top_k)
chunk_hits = self._search_chunk_ids(query_vec, top_k)
```
- 文件：`src/lineage_graphrag/retrieval/orchestrator.py`
- 类 / 函数：`LineageRetriever.retrieve`
- 关键代码：
```python
dual_result = self.dual.retrieve(graph=graph, chunks=chunks, question=question, top_k=top_k)
path1_results = dual_result["path1_results"]
path2_results = dual_result["path2_results"]
```
## 2. 它在主链路中的位置
它位于查询链路中。前面是 `/v1/queries/ask` 获取图和 chunks，并由 `AgenticIRCoT` 拆出子问题；后面是 `LineageRetriever` 聚合 triples 和 chunks，再交给 `AnswerGenerator` 生成答案。
## 3. 这段代码做了什么
`DualPathFAISSRetriever` 会先根据图规模和 chunks 数量判断索引是否需要重建。索引包括 node、relation、triple、community 和 chunk 五类。Path 1 是 `_path1_node_relation`，先用问题向量搜实体节点和关系文本，再扩展命中的节点一跳出入边，适合找明确实体附近的上下游。Path 2 是 `_path2_triple_community`，直接搜三元组和社区摘要，再从社区节点扩展 member、keyword 和代表实体，适合找更宽范围的语义簇和多跳上下文。最后再加一条 chunk 直接召回，把三路 chunk id 去重合并。
## 4. 输入和输出
### 输入
NetworkX 图、evidence chunks、自然语言问题、`top_k`。
### 输出
`path1_results`、`path2_results` 和合并后的 `chunk_ids`。上层 `LineageRetriever` 会进一步整理成 `triples`、`chunk_ids`、`chunk_contents`、`paths`。
## 5. 它有什么用
单一路径容易漏召回。只搜节点，能命中实体但可能漏掉关系语义；只搜三元组，能命中关系但可能缺少节点周边上下文；只搜 chunk，又容易丢图拓扑。双路检索把拓扑邻域和语义摘要结合起来，提高多跳依赖、指标链路、自然语言问答的上下文命中率。它也让系统在问题表达不完全等于节点名时，有机会通过 relation、triple 或 community 找到相关证据。
## 6. 调用链路
```text
AgenticIRCoT.run()
→ LineageRetriever.retrieve()
→ DualPathFAISSRetriever.retrieve()
→ _build_if_needed()
→ _path1_node_relation()
→ _path2_triple_community()
→ _search_chunk_ids()
→ LineageRetriever 聚合 triples/chunk_contents
```
## 7. 面试表达版本
双路检索我分成两类召回。第一路是 node + relation，先找和问题语义接近的实体或关系，再扩它的一跳出入边，适合问“某个字段/指标上下游是什么”。第二路是 triple + community，直接召回关系三元组和社区摘要，适合问题比较抽象或者跨多个节点的场景。最后我还会把 evidence chunk 直接召回结果合并进来。这样不是单纯靠向量文本，而是把图拓扑、关系语义和社区摘要一起用于召回。

# 二级问题：为什么要做社区检测和图摘要？
## 1. 代码位置
- 文件：`src/lineage_graphrag/graph/community_builder.py`
- 类 / 函数：`CommunityBuilder.build`
- 关键代码：
```python
projected = self._build_projected_entity_graph(graph, entity_nodes)
token_map = {node_id: self._entity_tokens(graph, node_id) for node_id in entity_nodes}
weighted_graph = self._build_dual_perceived_graph(projected, token_map)
communities = self._detect_communities(weighted_graph)
```
```python
graph.add_node(community_id, label="community", level=4, properties={...})
graph.add_edge(member, community_id, relation="member_of", ...)
self._create_keyword_nodes(graph, community_id, idx, keyword_entities)
```
- 文件：`src/lineage_graphrag/retrieval/dual_faiss_retriever.py`
- 类 / 函数：`DualPathFAISSRetriever._path2_triple_community`
## 2. 它在主链路中的位置
社区检测发生在建图末尾，社区摘要在检索阶段的 Path 2 被使用。前面是实体和依赖边构建完成，后面是查询时通过 community index 做更高层召回。
## 3. 这段代码做了什么
`CommunityBuilder` 先把 `has` 和 `transitions` 关系投影成实体无向图。然后为每个实体提取 name、description、schema_type 和 raw_properties 里的 token。接着用结构权重和语义相似度构建加权图，并调用 NetworkX 的 `greedy_modularity_communities` 做社区发现。每个社区会生成 community 节点，保存 members、representative、keyword_entities，并通过 `member_of`、`represents_community`、`represented_by`、`has_keyword` 等关系回连原图。
## 4. 输入和输出
### 输入
已经包含 entity、attribute、has、transitions 的 NetworkX 图。
### 输出
新增的 community 节点、keyword 节点，以及成员、代表、关键词相关边。
## 5. 它有什么用
血缘图规模大时，问题往往不是只问一个点，而是问某个业务域、指标组、数据链路簇。社区检测把局部强连接的实体聚合起来，社区描述和代表实体相当于图摘要。检索时如果用户没有说准节点名，也可能通过社区摘要命中相关子图。它提升的是粗粒度召回和多跳上下文覆盖能力。
## 6. 调用链路
```text
LineageKTBuilder.build()
→ CommunityBuilder.build(graph)
→ _build_projected_entity_graph()
→ _entity_tokens()
→ _build_dual_perceived_graph()
→ _detect_communities()
→ _keyword_representatives()
→ 新增 community/keyword 节点和边
→ DualPathFAISSRetriever._path2_triple_community() 使用 community index
```
## 7. 面试表达版本
社区检测主要是为了解决大图里“问题粒度比节点更粗”的情况。比如用户问一个指标域或者一条业务链路，直接搜单个节点可能命不中完整上下文。所以我在建图时把实体关系投影出来，结合结构连接和实体语义做社区发现，再把社区、代表实体和 keyword 节点写回图里。这样查询时可以通过社区摘要召回一组相关实体，而不是只拿到某个孤立节点。

# 二级问题：证据 chunks 是怎么生成、召回和重排的？
## 1. 代码位置
- 文件：`src/lineage_graphrag/ingest/evidence_builder.py`
- 类 / 函数：`EvidenceBuilder.build`
- 关键代码：
```python
chunks[f"entity::{entity.id}"] = json.dumps(entity.model_dump(), ensure_ascii=False)
chunks[f"transition::{transition.id}"] = json.dumps(transition.model_dump(), ensure_ascii=False)
chunks[f"subgraph::{entity.id}"] = json.dumps(payload, ensure_ascii=False)
```
- 文件：`src/lineage_graphrag/graph/kt_builder.py`
- 类 / 函数：`LineageKTBuilder.build`
- 关键代码：
```python
evidence_refs=[entity_chunk]
evidence_refs=[f"subgraph::{relation.source}"]
evidence_refs=[f"transition::{transition.id}"]
```
- 文件：`src/lineage_graphrag/retrieval/evidence_ranker.py`
- 类 / 函数：`rank_chunk_ids`
- 文件：`src/lineage_graphrag/retrieval/orchestrator.py`
- 类 / 函数：`LineageRetriever.retrieve`
## 2. 它在主链路中的位置
chunks 在建图时生成和绑定，在检索时被召回和重排，在答案生成时作为 prompt 证据输入给 LLM。
## 3. 这段代码做了什么
`EvidenceBuilder.build` 为每个 entity 生成 `entity::{id}` chunk，为每个 transition 生成 `transition::{id}` chunk；如果 entity 有 children，还生成 `subgraph::{id}` chunk。`LineageKTBuilder` 建边时把这些 chunk id 放到 `evidence_refs` 上。检索时，双路检索会从命中的节点、关系、三元组和社区边里收集 chunk ids，再加上 chunk 向量索引直接召回结果。`LineageRetriever` 最后调用 `rank_chunk_ids(question, chunks, all_chunk_ids, top_k)` 做重排，并把 chunk id 映射成 `chunk_contents`。
## 4. 输入和输出
### 输入
标准化 lineage、图边上的 `evidence_refs`、自然语言问题、候选 chunk ids。
### 输出
排序后的 `chunk_ids` 和对应的 `chunk_contents`，最后进入 `AnswerGenerator.generate`。
## 5. 它有什么用
chunks 是“答案可追溯”的基础。图边告诉系统关系存在，chunk 告诉系统关系从哪里来、原始结构是什么。如果没有 evidence chunk，LLM 只能看到三元组，回答会缺少上下文；如果没有重排，召回到的证据可能相关性不够稳定。当前实现比较轻量，适合面试时说明可以进一步替换成 cross-encoder 或业务规则重排。
## 6. 调用链路
```text
LineageKTBuilder.build()
→ EvidenceBuilder.build(normalized)
→ graph.add_edge(..., evidence_refs=[chunk_id])
→ DualPathFAISSRetriever.retrieve()
→ 收集 path1/path2/chunk index 的 chunk_ids
→ LineageRetriever.retrieve()
→ rank_chunk_ids()
→ chunk_contents
→ AnswerGenerator.generate()
```
## 7. 面试表达版本
我把证据做成了独立 chunks，而不是只存在图边里。建图时 entity、transition 和 subgraph 都会生成结构化 JSON chunk，边上会挂 `evidence_refs`。检索命中节点或关系后，就能沿着 evidence_refs 找回原始证据；同时也会对 chunk 本身做向量召回，最后再按问题相关性重排。这样最终给 LLM 的不是裸三元组，而是带出处的证据上下文，回答更可追溯。

# 二级问题：Agent 模式相比 noagent 模式提升在哪里？
## 1. 代码位置
- 文件：`src/lineage_graphrag/api/routes_query.py`
- 类 / 函数：`ask_question`
- 关键代码：
```python
mode = payload.mode or cfg.default_ask_mode
result = chain.run(..., mode=mode, max_steps=payload.max_steps)
```
- 文件：`src/lineage_graphrag/retrieval/agentic_ircot.py`
- 类 / 函数：`AgenticIRCoT.run`
- 关键代码：
```python
final_answer = self.answer_gen.generate(question, retrieval_result)
if normalized_mode == "noagent" or not self.answer_gen.llm_client.is_available():
    return {...}
```
```python
for step in range(1, loop_steps + 1):
    reasoning = self.answer_gen.llm_client.generate(prompt=ircot_prompt, ...)
    final = _extract_final_answer(reasoning)
    new_query = _extract_new_query(reasoning)
```
## 2. 它在主链路中的位置
它在查询问答链路中，包在问题拆解、检索、答案生成外层。前面是 API 拿到 graph/chunks，后面是返回 `AskResponse`。
## 3. 这段代码做了什么
无论 agent 还是 noagent，系统都会先做问题拆解，并对子问题执行一次检索，得到初始 `retrieval_result` 和 `final_answer`。如果 mode 是 `noagent`，或者 LLM client 不可用，就直接返回这个答案。agent 模式会继续进入 IRCoT 循环：每轮把已有 triples 和 chunks 拼成 context，让 LLM 判断证据是否足够；如果足够，输出 final answer；如果不够，输出 new query，系统用 new query 再检索，并把新旧结果去重合并。
## 4. 输入和输出
### 输入
图、chunks、原始问题、`top_k`、`mode`、`max_steps`。
### 输出
`answer`、`sub_questions`、`involved_types`、`retrieval`。agent 模式下 `retrieval.reasoning_steps` 会包含每轮 query、reasoning、new_query 和 found_final。
## 5. 它有什么用
noagent 是一次性检索加生成，延迟低、稳定、适合简单问题。agent 模式的价值是让复杂问题可以“边检索边修正查询”，特别是初始问题过长、包含多个约束或第一轮证据不足时，有机会通过新查询补证据。工程上也做了降级：LLM 不可用时自动走 noagent 返回，不会让接口失败。
## 6. 调用链路
```text
POST /v1/queries/ask
→ ask_question()
→ AgenticIRCoT.run(mode)
→ LineageQuestionDecomposer.decompose()
→ LineageRetriever.retrieve()
→ AnswerGenerator.generate()
→ noagent: 直接返回
→ agent: build_ircot_prompt()
→ LLMClient.generate()
→ _extract_final_answer() / _extract_new_query()
→ 继续检索或返回答案
```
## 7. 面试表达版本
我这里的 agent 模式不是一上来就让 LLM 自由发挥，而是在一次基础检索之后，再做 IRCoT 式的迭代。noagent 模式就是子问题拆解、检索、生成一次完成，适合简单问题。agent 模式会让模型基于当前证据判断是否够回答，不够就产出新的检索 query，再把新证据合并进来。它的提升点是复杂血缘问题可以多轮补证据，同时我也做了降级，LLM 不可用时会直接返回 noagent 的结果。

# 二级问题：IRCoT 的多轮检索推理在代码里怎么实现？
## 1. 代码位置
- 文件：`src/lineage_graphrag/retrieval/agentic_ircot.py`
- 类 / 函数：`AgenticIRCoT.run`
- 关键代码：
```python
context = _build_context(retrieval_result)
ircot_prompt = build_ircot_prompt(...)
reasoning = self.answer_gen.llm_client.generate(...)
final = _extract_final_answer(reasoning)
new_query = _extract_new_query(reasoning)
```
```python
iter_result = self.retriever.retrieve(..., question=current_query, ...)
retrieval_result = _merge_retrieval_results(retrieval_result, iter_result, top_k=top_k)
final_answer = self.answer_gen.generate(question, retrieval_result)
```
- 文件：`src/lineage_graphrag/llm/prompts.py`
- 类 / 函数：`build_ircot_prompt`
## 2. 它在主链路中的位置
它发生在查询链路的检索和最终返回之间。前面已经有初始检索结果，后面是合并后的证据和最终答案。
## 3. 这段代码做了什么
IRCoT 循环最多执行 `max_steps` 次。每轮先用 `_build_context` 从当前检索结果里取前 12 个 triples 和前 8 个 chunks，构造 prompt。prompt 要求 LLM 如果证据足够，以 `So the answer is:` 结尾；如果证据不足，以 `The new query is:` 结尾。代码用正则 `_extract_final_answer` 和 `_extract_new_query` 解析输出。如果拿到 final answer 就结束；如果拿到 new query，就用它重新检索，再用 `_merge_retrieval_results` 去重合并 triples、chunk_ids、chunk_contents 和 paths。
## 4. 输入和输出
### 输入
初始 `retrieval_result`、原始问题、当前 query、历史 thoughts、`max_steps`。
### 输出
更新后的 `final_answer`、合并后的 `retrieval_result`、每轮 `reasoning_steps`。
## 5. 它有什么用
IRCoT 的核心价值是把“推理”和“检索”交替进行。很多血缘问题第一轮检索只命中部分节点，模型可以根据已有证据生成更具体的新查询，再去图里补上下文。不过当前实现依赖固定文本格式和正则解析，鲁棒性一般；面试时可以说后续可改成结构化 JSON 输出或 tool call，减少解析失败。
## 6. 调用链路
```text
AgenticIRCoT.run()
→ 初始 LineageRetriever.retrieve()
→ _build_retrieval_result()
→ AnswerGenerator.generate()
→ for step in max_steps
→ _build_context()
→ build_ircot_prompt()
→ LLMClient.generate()
→ _extract_final_answer()
→ _extract_new_query()
→ LineageRetriever.retrieve(new_query)
→ _merge_retrieval_results()
→ AnswerGenerator.generate()
```
## 7. 面试表达版本
IRCoT 这块我实现成了一个受控循环，不是无限 agent。先做一轮基础检索和答案生成，然后每轮把当前 triples 和 chunks 作为 context 给 LLM。模型要么说证据够了，按固定格式给 final answer；要么给一个更好的 new query。系统拿 new query 再去检索，并把新旧证据去重合并。这个机制的作用是让复杂血缘问题可以逐步补齐证据，但我也知道现在用正则解析格式，后续更适合改成 JSON schema 或函数调用。

# 二级问题：答案为什么说是可追溯的？追溯到哪里？
## 1. 代码位置
- 文件：`src/lineage_graphrag/graph/kt_builder.py`
- 类 / 函数：`LineageKTBuilder.build`
- 关键代码：
```python
evidence_refs=[entity_chunk]
evidence_refs=[f"subgraph::{relation.source}"]
evidence_refs=[f"transition::{transition.id}"]
```
- 文件：`src/lineage_graphrag/retrieval/orchestrator.py`
- 类 / 函数：`LineageRetriever.retrieve`
- 关键代码：
```python
dedup_triples = list(dict.fromkeys(triples))[: max(top_k * 2, top_k)]
ranked_chunk_ids = rank_chunk_ids(question, chunks, all_chunk_ids, top_k=max(top_k * 2, top_k))
chunk_contents = [chunks[cid] for cid in ranked_chunk_ids if cid in chunks]
```
- 文件：`src/lineage_graphrag/llm/answer_generator.py`
- 类 / 函数：`AnswerGenerator.generate`
- 关键代码：
```python
prompt = build_answer_prompt(question, triples, chunks, impact_summary)
system_prompt="You are a strict lineage analysis assistant. Answer only from provided evidence."
```
## 2. 它在主链路中的位置
可追溯性贯穿建图、检索和答案生成。建图时挂 evidence refs，检索时返回 triples 和 chunks，生成时限制答案只能基于提供的证据。
## 3. 这段代码做了什么
建图时每条关键边会绑定证据来源：属性边追溯到 entity chunk，层级边追溯到 subgraph chunk，流转边追溯到 transition chunk。检索阶段根据命中的节点、关系、三元组和社区收集 chunk id，并重排成 `chunk_contents`。回答阶段 prompt 会把 triples 和 chunks 一起给 LLM，并在 system prompt 中要求只能基于证据回答。接口响应里还会返回 `retrieval`，调用方可以看到命中的 triples、chunk_ids、chunk_contents 和 reasoning_steps。
## 4. 输入和输出
### 输入
图边上的 `evidence_refs`、chunks 字典、用户问题。
### 输出
答案文本，以及可审计的 `retrieval` 字段，包含证据三元组、证据 chunk id 和内容。
## 5. 它有什么用
治理类问答不能只给一个自然语言结论，还要能解释结论来自哪条血缘边、哪个实体或哪个 transition。可追溯性降低了 LLM 幻觉风险，也方便排查错误召回。当前接口已经返回 chunk 内容，但还可以优化为返回更结构化的 citation，例如每句话绑定 chunk id。
## 6. 调用链路
```text
EvidenceBuilder.build()
→ LineageKTBuilder.build() 把 chunk id 写到 evidence_refs
→ DualPathFAISSRetriever 收集 evidence_refs
→ LineageRetriever.rank_chunk_ids()
→ AnswerGenerator.build_answer_prompt()
→ AskResponse.retrieval 返回 triples/chunk_ids/chunk_contents
```
## 7. 面试表达版本
我说答案可追溯，是因为图里的关系不是孤立三元组。建图时每条边都会挂 `evidence_refs`，比如 transition 边能追溯到 `transition::{id}`，层级关系能追溯到 `subgraph::{entity_id}`。检索命中关系后，会把这些 chunk id 找回来并放进 prompt，接口也会把 chunk_ids 和 chunk_contents 返回。这样用户不仅看到答案，还能看到答案依据的是哪些血缘关系和原始结构化证据。

# 二级问题：what-if 影响分析是怎么找下游影响路径的？
## 1. 代码位置
- 文件：`src/lineage_graphrag/api/routes_impact.py`
- 类 / 函数：`what_if_impact`
- 关键代码：
```python
graph = repo.get_graph(payload.graph_id)
report = analyzer.analyze(
    graph=graph,
    change_spec=payload.change_spec,
    target_node_id=payload.target_node_id,
)
```
- 文件：`src/lineage_graphrag/impact/propagation.py`
- 类 / 函数：`ImpactAnalyzer.analyze`、`_downstream_paths`、`_target_impact`、`_scope_impact`
- 关键代码：
```python
paths = self._downstream_paths(graph, change_spec.target, max_depth=change_spec.max_depth)
direct = sorted({p.path[-1] for p in paths if p.depth == 1})
indirect = sorted({p.path[-1] for p in paths if p.depth > 1})
```
```python
for _, nxt, edge_data in graph.out_edges(node, data=True):
    paths.append(ImpactPath(path=next_nodes, relations=next_relations, depth=next_depth))
```
## 2. 它在主链路中的位置
它是治理场景落地链路。前面是图已经通过导入链路构建并保存，后面是返回直接影响、间接影响、目标路径、范围过滤结果和 evidence paths。
## 3. 这段代码做了什么
接口先根据 `graph_id` 从 repository 取图，找不到就返回 404。`ImpactAnalyzer.analyze` 从 `change_spec.target` 开始做下游路径搜索。`_downstream_paths` 使用队列 BFS，初始状态是目标节点、路径列表、关系列表和深度 0；每次遍历当前节点的所有出边，把下一个节点追加到路径里，并记录边关系和深度。搜索结果按 depth 分成 direct impacts 和 indirect impacts。如果传了 `target_node_id`，就筛选终点等于目标节点的路径。如果传了 scope，就根据节点 raw_properties 里的 domain 做范围过滤。
## 4. 输入和输出
### 输入
`ImpactRequest`：`graph_id`、`change_spec`、可选 `target_node_id`。其中 `change_spec` 包含变更目标、最大深度和 scope。
### 输出
`ImpactReport`：`direct_impacts`、`indirect_impacts`、`target_impact`、`scoped_impact`、`evidence_paths`。
## 5. 它有什么用
what-if 分析回答的是“如果某个字段、指标或节点变了，会影响谁”。图的有向边天然表达依赖方向，BFS 可以按层找到直接和间接影响路径。`max_depth` 控制搜索范围，避免大图里无限扩散；`target_node_id` 支持判断某个关键节点是否受影响；`scope` 支持按业务域过滤。当前实现没有按边类型过滤，面试时可以说明后续可以只沿 `transitions` 传播，避免 `has_attribute` 等非依赖边造成噪声。
## 6. 调用链路
```text
POST /v1/impact/what-if
→ what_if_impact(payload)
→ GraphRepository.get_graph(graph_id)
→ ImpactAnalyzer.analyze()
→ _downstream_paths(change_spec.target, max_depth)
→ _target_impact(paths, target_node_id)
→ _scope_impact(graph, impacted_nodes, scope)
→ ImpactReport.model_dump()
```
## 7. 面试表达版本
what-if 这块本质是图上的下游传播。接口拿到变更目标后，从 `change_spec.target` 开始沿出边做 BFS，每走一层就记录路径节点、边关系和深度。深度为 1 的是直接影响，大于 1 的是间接影响。用户如果关心某个目标节点，我会检查有没有路径能到它；如果指定 scope，就按节点的 domain 做过滤。这个能力适合字段变更、指标口径调整这类治理场景，不过当前可以继续优化，比如只沿真正的数据流转边传播。

# 二级问题：你简历里的 Top-5 命中率 78%、人工评测 82% 是怎么评估出来的？
## 1. 代码位置
- 文件：`src/lineage_graphrag/evaluation/retrieval_eval.py`
- 类 / 函数：`retrieval_smoke_check`
- 关键代码：
```python
def retrieval_smoke_check(triples: list[str]) -> bool:
    return len(triples) > 0
```
- 文件：`src/lineage_graphrag/evaluation/answer_eval.py`
- 类 / 函数：`answer_smoke_check`
- 关键代码：
```python
def answer_smoke_check(answer: str) -> bool:
    return bool(answer.strip())
```
- 文件：`tests/unit/test_dual_path_retrieval.py`
- 类 / 函数：检索相关单测
- 文件：`tests/integration/test_api.py`
- 类 / 函数：API 集成测试
## 2. 它在主链路中的位置
评测不在在线主链路里，而是围绕查询链路的离线验证和测试。前面是构造测试图和问题集，后面是统计 retrieval 命中和 answer 质量。
## 3. 这段代码做了什么
当前仓库里的 `evaluation` 模块还是 smoke check 级别，只检查检索是否返回 triples、答案是否非空；真正简历里的 Top-5 证据命中率和人工通过率，更像是基于内部评测集的离线统计结果，而不是完整沉淀在当前代码里。按照项目已有返回结构，可以用 `/v1/queries/ask` 的 `retrieval.chunk_ids` 或 `retrieval.triples` 与标注 gold evidence 对比，计算 Top-5 是否命中；生成回答人工评测则可以按“是否基于证据、是否回答问题、是否无明显幻觉”等维度打通过/不通过。
## 4. 输入和输出
### 输入
内部问题评测集，每条样本至少包含 question、graph_id、gold evidence ids 或 gold triples，以及人工评测标准。
### 输出
Top-5 evidence hit rate，例如 `命中样本数 / 总样本数 = 78%`；生成答案人工通过率，例如 `人工判定通过答案数 / 总答案数 = 82%`。
## 5. 它有什么用
这组指标证明系统不是只“能跑通”，而是在证据召回和回答质量上有可量化效果。Top-5 命中率衡量检索是否把正确证据放进前几条，人工通过率衡量答案是否真正可用。需要注意的是，当前开源代码里的 evaluation 还比较简化，面试时不要说仓库里已经完整实现了 78%/82% 的评测脚本，可以表达为内部评测集统计，并说明如果工程化，会把 gold evidence、自动指标和人工标注流程固化到 evaluation 模块。
## 6. 调用链路
```text
评测样本(question, graph_id, gold_evidence)
→ /v1/queries/ask 或 AgenticIRCoT.run()
→ retrieval.chunk_ids / retrieval.triples
→ Top-5 与 gold_evidence 对比
→ answer 人工判定
→ 汇总 hit rate 和 pass rate
```
## 7. 面试表达版本
这两个指标我会这样解释：Top-5 命中率是看系统返回的前 5 个证据里有没有覆盖人工标注的关键证据，比如关键 chunk id 或关键三元组；人工通过率是看最终回答是否回答了问题、是否能被证据支撑、有没有明显幻觉。当前代码里的 evaluation 模块还是 smoke check，更完整的 78% 和 82% 是基于内部问题集统计出来的。这个点面试时要讲清楚，避免把实验统计说成线上实时指标。

# 二级问题：当非结构化文本进来后，如何生成图的？
## 1. 代码位置
- 文件：`src/lineage_graphrag/api/routes_ingest.py`
- 类 / 函数：`import_lineage`、`_materialize_graph`
- 关键代码：
```python
lineage = parser.parse(payload.lineage_json)
normalized = normalizer.normalize(lineage)
repo.save_lineage(payload.graph_id, normalized)
build_result = _materialize_graph(payload.graph_id, request, repo)
```
```python
result = builder.build(normalized)
mirror_status = repo.save_graph(graph_id, result.graph, result.evidence_chunks, result.metadata)
snapshot_store.save(graph_id, result.graph, result.evidence_chunks)
```
- 文件：`src/lineage_graphrag/ingest/parser.py`
- 类 / 函数：`LineageParser.parse`、`_canonical_entities`、`_canonical_transitions`、`_validate_cross_refs`
- 关键代码：
```python
canonical["entities"] = self._canonical_entities(raw_json.get("entities", {}))
canonical["transitions"] = self._canonical_transitions(raw_json.get("transitions", []))
lineage = LineageInput.model_validate(canonical)
self._validate_cross_refs(lineage)
```
- 文件：`src/lineage_graphrag/ingest/normalizer.py`
- 类 / 函数：`LineageNormalizer.normalize`
- 关键代码：
```python
for entity in entities:
    for child in entity.children:
        has_relations.append(
            HasRelation(source=entity.id, target=child, properties={"origin": "children"})
        )
```
- 文件：`src/lineage_graphrag/graph/kt_builder.py`
- 类 / 函数：`LineageKTBuilder.build`
- 关键代码：
```python
graph = nx.MultiDiGraph()
chunks = self.evidence_builder.build(lineage)
graph.add_node(entity_id, label="entity", level=2, ...)
graph.add_edge(relation.source, relation.target, relation="has", ...)
graph.add_edge(transition.source, transition.target, relation="transitions", ...)
self.community_builder.build(graph)
```
- 注意：当前仓库主路径里没有单独的 `document/text ingest` 接口或文档解析器文件；代码层面真正进入建图器的是已经抽取好的 `lineage_json`。所以非结构化文本输入的关键前置步骤，是先把文本中的实体、字段释义、指标口径、上下游依赖抽取成项目的 canonical lineage schema。
## 2. 它在主链路中的位置
它位于导入建图链路的最前面。前面是用户上传或输入非结构化文本，例如需求文档、指标口径文档、接口说明；中间需要经过文档解析/信息抽取，把文本转成 `entities`、`transitions`、`children` 或 `has_relations`；后面复用已有的 `LineageParser -> LineageNormalizer -> LineageKTBuilder -> Repository/Snapshot/FalkorDB` 建图链路。
## 3. 这段代码做了什么
执行顺序可以分成两段。第一段是非结构化文本到结构化 lineage：从文档中识别业务实体，例如字段、表、指标、服务、接口、文档章节；再抽取上下游依赖，例如“指标 A 由字段 B 计算得来”“接口 X 消费表 Y”“字段变更会影响报表 Z”。抽取结果需要组织成项目支持的 lineage JSON：`entities` 是以 id 为 key 的对象 map，每个实体带 name、description、properties、children；`transitions` 表示实体之间的数据流转或依赖边，包含 id、source、target、properties。

第二段是结构化 lineage 到图。`LineageParser.parse` 负责把输入 canonical 化，并校验 children、transition source/target 都引用已有 entity。`LineageNormalizer.normalize` 把隐含的 `children` 转成显式 `has_relations`。`LineageKTBuilder.build` 创建 `MultiDiGraph`，把实体建成 entity 节点，把属性和描述建成 attribute 节点，把 `has_relations` 写成 `has` 边，把 `transitions` 写成 `transitions` 边，并生成 evidence chunks。最后 `CommunityBuilder` 增加 keyword/community 层，`GraphRepository` 保存内存图，`SnapshotStore` 写快照，FalkorDB 做可选镜像持久化。
## 4. 输入和输出
### 输入
非结构化文本或文档内容，例如指标说明、字段口径、需求文档、接口文档。经过抽取后，应转换为项目已有的 `lineage_json`：
```json
{
  "entities": {
    "metric_gmv": {
      "id": "metric_gmv",
      "name": "GMV",
      "description": "成交金额指标",
      "properties": {"type": "metric", "domain": "trade"},
      "children": []
    }
  },
  "transitions": [
    {
      "id": "tr::order_amount->metric_gmv",
      "source": "order_amount",
      "target": "metric_gmv",
      "properties": {"source": "document", "rule": "sum order amount"}
    }
  ]
}
```
### 输出
输出是 `GraphBuildResult` 和持久化后的图：包括 NetworkX `MultiDiGraph`、evidence chunks、metadata、snapshot 文件，以及可选的 FalkorDB 图。接口层最终返回 graph_id、节点/关系统计、chunks 数量和 FalkorDB 写入状态。
## 5. 它有什么用
这个节点的工程意义是把“文档里的隐含知识”接入统一血缘图，而不是让文档只停留在普通 RAG 的文本片段里。文档里经常写着字段口径、指标计算规则、接口依赖和业务约束，如果不抽取成实体和边，系统只能做语义搜索，无法做上下游路径查询、影响传播和图谱证据追溯。先抽取成 lineage schema，再复用统一建图链路，可以让结构化元数据和非结构化文档进入同一张图，后续的双路检索、IRCoT 问答和 what-if 分析都能复用。

如果没有这一步，文档输入最多只能作为 chunk 被召回，不能参与图拓扑计算；例如“某个字段改了会影响哪些指标”这种问题，很难从纯文本 RAG 中稳定得到可计算路径。这个设计的价值是把非结构化文档变成可计算、可遍历、可追溯的图结构。
## 6. 调用链路
```text
非结构化文本 / 文档
→ 文档解析与信息抽取
→ 抽取实体 entities
→ 抽取上下游依赖 transitions / children / has_relations
→ 生成 canonical lineage_json
→ POST /v1/graphs/import
→ import_lineage()
→ LineageParser.parse()
→ LineageNormalizer.normalize()
→ LineageKTBuilder.build()
→ EvidenceBuilder.build()
→ CommunityBuilder.build()
→ GraphRepository.save_graph()
→ SnapshotStore.save()
→ FalkorDBClient.write_graph()
```
## 7. 面试表达版本
非结构化文档进来以后，我不是直接把整篇文档塞给向量库，而是先做一层信息抽取，把文档里的字段、指标、表、接口这些对象识别成实体，再把“由什么计算得来”“依赖哪个表”“影响哪个下游”这类描述抽成 transition 或 children 关系。抽取结果会转成项目统一的 lineage JSON，然后复用原来的导入链路：parser 做格式和引用校验，normalizer 把隐含层级转成 has 关系，builder 再生成四层 `MultiDiGraph`，同时生成 evidence chunks 并持久化。这样文档知识就不只是文本证据，而是进入了可遍历的血缘图，后面自然语言问答、指标依赖分析和 what-if 影响分析都可以复用同一套 GraphRAG 能力。

# 二级问题：如果用 LlamaIndex 做非结构化文档抽取，应该如何接入现有建图链路？
## 1. 代码位置
- 当前已有复用链路文件：`src/lineage_graphrag/api/routes_ingest.py`
- 类 / 函数：`import_lineage`、`_materialize_graph`
- 关键代码：
```python
lineage = parser.parse(payload.lineage_json)
normalized = normalizer.normalize(lineage)
repo.save_lineage(payload.graph_id, normalized)
build_result = _materialize_graph(payload.graph_id, request, repo)
```
- 当前已有 schema 文件：`src/lineage_graphrag/domain/lineage_models.py`
- 类 / 函数：`EntityInput`、`TransitionInput`、`HasRelation`、`LineageInput`
- 关键代码：
```python
class EntityInput(BaseModel):
    id: str
    name: str = ""
    properties: dict[str, Any] = Field(default_factory=dict)
    description: str = ""
    children: list[str] = Field(default_factory=list)

class TransitionInput(BaseModel):
    id: str
    source: str
    target: str
    properties: dict[str, Any] = Field(default_factory=dict)
```
- 建议新增文件：`src/lineage_graphrag/ingest/document_extractor.py`
- 建议新增类 / 函数：`LlamaIndexLineageExtractor.extract`
- 建议关键代码：
```python
from typing import Literal
from llama_index.core import Document, PropertyGraphIndex
from llama_index.core.indices.property_graph import SchemaLLMPathExtractor

EntityType = Literal["FIELD", "TABLE", "METRIC", "SERVICE", "API", "DOCUMENT_SECTION"]
RelationType = Literal["HAS", "TRANSITIONS"]

validation_schema = [
    ("DOCUMENT_SECTION", "HAS", "METRIC"),
    ("DOCUMENT_SECTION", "HAS", "FIELD"),
    ("DOCUMENT_SECTION", "HAS", "TABLE"),
    ("DOCUMENT_SECTION", "HAS", "API"),
    ("DOCUMENT_SECTION", "HAS", "SERVICE"),
    ("FIELD", "TRANSITIONS", "METRIC"),
    ("TABLE", "TRANSITIONS", "FIELD"),
    ("API", "TRANSITIONS", "FIELD"),
    ("SERVICE", "TRANSITIONS", "TABLE"),
    ("METRIC", "TRANSITIONS", "SERVICE"),
]

extractor = SchemaLLMPathExtractor(
    llm=llm,
    possible_entities=EntityType,
    possible_relations=RelationType,
    kg_validation_schema=validation_schema,
    strict=True,
)

index = PropertyGraphIndex.from_documents(
    [Document(text=text, metadata={"document_id": document_id})],
    kg_extractors=[extractor],
    show_progress=True,
)
```
- 建议新增文件：`src/lineage_graphrag/ingest/lineage_adapter.py`
- 建议新增类 / 函数：`PropertyGraphToLineageAdapter.to_lineage_json`
- 建议关键代码：
```python
def to_lineage_json(paths: list[ExtractedPath], document_id: str) -> dict:
    entities = {}
    transitions = []
    children_by_parent = {}
    for path in paths:
        source_id = normalize_id(path.source_type, path.source_name)
        target_id = normalize_id(path.target_type, path.target_name)
        entities[source_id] = build_entity(source_id, path.source_name, path.source_type, document_id)
        entities[target_id] = build_entity(target_id, path.target_name, path.target_type, document_id)
        relation = path.relation.lower()
        if relation == "has":
            children_by_parent.setdefault(source_id, []).append(target_id)
            continue
        transitions.append({
            "id": f"tr::{source_id}->{target_id}",
            "source": source_id,
            "target": target_id,
            "properties": {
                "relation": "transitions",
                "business_semantics": infer_business_semantics(path.evidence_text),
                "source": "llamaindex_document_extraction",
                "document_id": document_id,
                "chunk_id": path.chunk_id,
                "evidence_text": path.evidence_text,
                "confidence": path.confidence,
            },
        })
    for parent_id, child_ids in children_by_parent.items():
        entities[parent_id]["children"] = sorted(set(child_ids))
    return {"entities": entities, "transitions": transitions}
```
- 官方依据：LlamaIndex 的 `PropertyGraphIndex` 支持在文档 chunk 上运行 `kg_extractors`，`SchemaLLMPathExtractor` 可以指定实体类型、关系类型和 `kg_validation_schema`，并用 `strict=True` 限制抽取结果落在 schema 内。
## 2. 它在主链路中的位置
这个 LlamaIndex 抽取层应该放在现有 `/v1/graphs/import` 之前。前面是非结构化文档输入，例如需求文档、指标口径文档、接口说明；中间用 LlamaIndex 做 schema 约束的实体关系抽取，并通过 adapter 转成项目自己的 lineage JSON；后面完全复用现有 `LineageParser -> LineageNormalizer -> LineageKTBuilder -> GraphRepository -> SnapshotStore/FalkorDB` 链路。
## 3. 这段代码做了什么
执行顺序可以拆成 4 步。第一步，文档解析和分块，把 PDF、Word、Markdown 或纯文本转成 LlamaIndex `Document` 或 node/chunk，同时保留 `document_id`、页码、章节标题、chunk_id 等来源信息。第二步，定义血缘抽取 schema：实体类型可以限制为 `FIELD`、`TABLE`、`METRIC`、`SERVICE`、`API`、`DOCUMENT_SECTION`，关系类型要和当前项目保持一致，只允许 `HAS` 和 `TRANSITIONS`。如果文档里出现 computed_from、depends_on、consumes、produces、impacts 这类更细的业务语义，不作为图的 relation 类型，而是放进 `transition.properties.business_semantics`。第三步，用 `SchemaLLMPathExtractor` 从每个 chunk 里抽取符合 schema 的实体关系路径，例如 `(DOCUMENT_SECTION:GMV口径)-[HAS]->(METRIC:GMV)` 或 `(FIELD:order_amount)-[TRANSITIONS]->(METRIC:GMV)`。第四步，用 adapter 把 LlamaIndex 抽出来的 path/triple 转成项目已有的 `lineage_json`：`HAS` 关系可以进入 entity 的 `children` 或显式 `has_relations`，`TRANSITIONS` 关系进入 `transitions`，原文证据、chunk_id、document_id、confidence 和细分业务语义进入 `transition.properties`。

这里要注意，LlamaIndex 负责的是“从文本中抽 property graph 路径”，不是直接生成本项目最终图。最终图仍然由 `LineageParser` 校验、`LineageNormalizer` 规范化、`LineageKTBuilder` 构建四层血缘图，这样结构化元数据和文档抽取结果能统一进入同一套图模型。
## 4. 输入和输出
### 输入
非结构化文档文本和抽取配置：
```json
{
  "graph_id": "trade_governance",
  "document_id": "metric_doc_001",
  "text": "GMV 指标由 order_amount 汇总得到，下游用于交易看板 trade_dashboard。",
  "schema": {
    "entities": ["FIELD", "TABLE", "METRIC", "SERVICE", "API"],
    "relations": ["HAS", "TRANSITIONS"]
  }
}
```
### 输出
adapter 输出项目已有的 lineage JSON：
```json
{
  "entities": {
    "metric_gmv": {
      "id": "metric_gmv",
      "name": "GMV",
      "description": "从文档 metric_doc_001 抽取的指标实体",
      "properties": {"type": "metric", "source": "document", "document_id": "metric_doc_001"},
      "children": []
    },
    "field_order_amount": {
      "id": "field_order_amount",
      "name": "order_amount",
      "description": "从文档 metric_doc_001 抽取的字段实体",
      "properties": {"type": "field", "source": "document", "document_id": "metric_doc_001"},
      "children": []
    }
  },
  "transitions": [
    {
      "id": "tr::field_order_amount->metric_gmv",
      "source": "field_order_amount",
      "target": "metric_gmv",
      "properties": {
        "relation": "transitions",
        "business_semantics": "computed_from",
        "source": "llamaindex_document_extraction",
        "document_id": "metric_doc_001",
        "evidence_text": "GMV 指标由 order_amount 汇总得到",
        "confidence": 0.86
      }
    }
  ]
}
```
后续输出就是现有建图链路的结果：`GraphBuildResult`、NetworkX 四层图、evidence chunks、snapshot 和 FalkorDB 持久化结果。
## 5. 它有什么用
LlamaIndex 这层解决的是“非结构化文档怎么变成可计算血缘关系”的问题。直接把文档放进向量库只能做文本相似度检索，无法稳定支持路径查询、下游传播和影响分析。`SchemaLLMPathExtractor` 的价值是可以把 LLM 的自由抽取限制在业务允许的实体类型、关系类型和连接规则里，减少把无关概念抽进图里的概率。adapter 的价值是把第三方框架的 property graph 输出转成项目自己的 lineage schema，避免后续建图、检索、影响分析被 LlamaIndex 绑定死。

工程上还需要加三类兜底。第一是 schema 校验，source/target 必须存在，关系类型必须合法；第二是实体归一化，同一个字段可能有中文名、英文名、别名和大小写差异；第三是人工审核或置信度阈值，低 confidence 的边可以先进入待确认状态。这样可以兼顾自动化抽取和血缘图质量。
## 6. 调用链路
```text
非结构化文档
→ DocumentLoader / DocumentChunker
→ LlamaIndex Document
→ SchemaLLMPathExtractor(possible_entities, possible_relations, kg_validation_schema, strict=True)
→ PropertyGraphIndex.from_documents(..., kg_extractors=[extractor])
→ 抽取 property graph paths / triples
→ PropertyGraphToLineageAdapter.to_lineage_json()
→ LineageParser.parse(lineage_json)
→ LineageNormalizer.normalize()
→ LineageKTBuilder.build()
→ EvidenceBuilder.build()
→ CommunityBuilder.build()
→ GraphRepository.save_graph()
→ SnapshotStore.save()
→ FalkorDBClient.write_graph()
```
## 7. 面试表达版本
如果要把文档输入补完整，我会把 LlamaIndex 放在现有建图链路前面，专门负责非结构化文本到实体关系的抽取。我会先定义业务 schema，比如实体只允许字段、表、指标、接口、服务，但关系类型和项目当前图模型保持一致，只允许 `has` 和 `transitions`。文档里更细的语义，比如 computed_from、depends_on、consumes、produces，不直接作为图 relation，而是放到 `transition.properties` 里。LlamaIndex 的 `SchemaLLMPathExtractor` 按这个 schema 从文档 chunk 里抽 property graph path，后面 adapter 再转成项目已有的 lineage JSON：`has` 进入 children 或 has_relations，`transitions` 进入 transitions，同时保留 document_id、chunk_id、evidence_text 和 confidence。这样后面仍然复用原来的 parser、normalizer、四层图构建和 FalkorDB 持久化，既接入非结构化文档，又不破坏现有血缘图模型。
