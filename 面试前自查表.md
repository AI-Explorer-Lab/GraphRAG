# 面试前自查表

## 1. 主链路理解程度

### 核心流程

1. 启动：`create_app()` 读取配置，初始化 `GraphRepository`、`SnapshotStore`，并从 snapshot / FalkorDB hydrate 图数据。
   - `src/lineage_graphrag/api/app.py`

2. 导入：`POST /v1/graphs/import` 接收 lineage JSON，经过 `LineageParser -> LineageNormalizer -> LineageKTBuilder`，生成 `NetworkX MultiDiGraph` 和 evidence chunks，然后写入内存、snapshot，并可选 mirror 到 FalkorDB。
   - `src/lineage_graphrag/api/routes_ingest.py`

3. 查询：`POST /v1/queries/ask` 从 repo 取 graph/chunks，进入 `AgenticIRCoT`。先问题分解，再双路检索，再生成答案；`agent` 模式且 LLM 可用时会跑 IRCoT 迭代，否则退化为一次检索答案。
   - `src/lineage_graphrag/api/routes_query.py`
   - `src/lineage_graphrag/retrieval/agentic_ircot.py`

4. 检索：`DualPathFAISSRetriever` 建 5 类索引：node、relation、triple、community、chunk。Path1 是 node + relation，Path2 是 triple + community。
   - `src/lineage_graphrag/retrieval/dual_faiss_retriever.py`

5. 影响分析：`POST /v1/impact/what-if` 使用 BFS 从 `change_spec.target` 往下游走，返回 direct / indirect / scoped impact。
   - `src/lineage_graphrag/impact/propagation.py`

### 讲不清会被判定为不合格的点

- 讲不清 import 到 query 的完整链路，只会说“用了 GraphRAG / Agent”。
- 把 FalkorDB 说成主存储；实际主路径是内存 repo，FalkorDB 是 optional mirror / hydrate 来源。
- 讲不清 `has` 和 `transitions` 的区别：`has` 来自 children 层级，`transitions` 来自数据流转。
- 讲不清 agent 模式什么时候真正跑 LLM IRCoT：只有 `mode=agent` 且 `llm_client.is_available()`。
- 讲错影响分析起点：当前代码从 `change_spec.target` 开始传播，不是从 `source` 开始。

## 2. 技术选型理解

### 关键技术为什么这样选

- FastAPI + Pydantic：接口少、类型约束清楚，适合快速把导入、查询、影响分析暴露成服务。
- NetworkX MultiDiGraph：适合原型阶段表达多类型节点、多关系边、BFS/社区发现；不是生产级大图存储。
- FalkorDB：用于图数据库 mirror、重启恢复和外部可视化，不是在线查询主依赖。
- FAISS / sentence-transformers：做语义检索；同时有 numpy / hashed BOW fallback，避免离线环境直接不可用。
- Agentic IRCoT：用于多跳问题的分解、迭代检索和答案修正；但项目保留 `noagent` 和本地 fallback，降低 LLM 不可用风险。
- CommunityBuilder：把原始血缘图扩展到 community / keyword / representative 层，服务更高层的召回。

### 面试官最可能追问的“为什么”

- 为什么不用纯向量 RAG？
  - 血缘问题强依赖方向、上下游、关系类型和证据路径，必须保留图结构。

- 为什么不用 Neo4j / Nebula 直接做主存储？
  - 当前项目定位是可控原型，NetworkX 保证构图和测试简单；FalkorDB 只是持久化镜像。

- 为什么要双路检索？
  - Path1 覆盖实体附近一跳关系，Path2 覆盖显式 triple 和 community 召回，兼顾精确节点命中和高层语义聚合。

- 为什么 LLM 要可选？
  - 系统需要在无 API key、离线、CI 环境下仍能返回基于证据的可解释结果。

- 为什么做 community / keyword 节点？
  - 多跳血缘图里，用户问题常是业务语义而非精确节点名，community 层提高粗粒度召回。

## 3. 必须掌握的代码级细节

### 必须能读懂/讲清的模块

- `src/lineage_graphrag/ingest/parser.py`
  - 输入 canonical 化、实体/transition 交叉引用校验、旧 list 格式兼容。

- `src/lineage_graphrag/ingest/normalizer.py`
  - 从 `children` 物化 `has_relations`。

- `src/lineage_graphrag/graph/kt_builder.py`
  - entity / attribute / has / transitions / community 的构图逻辑。

- `src/lineage_graphrag/graph/community_builder.py`
  - 投影实体图、结构+语义融合、社区检测、代表节点。

- `src/lineage_graphrag/retrieval/dual_faiss_retriever.py`
  - 索引构建、缓存 signature、Path1 / Path2 检索。

- `src/lineage_graphrag/retrieval/agentic_ircot.py`
  - 问题分解、初始检索、IRCoT 循环、新 query 抽取、最终答案抽取。

- `src/lineage_graphrag/storage/graph_repository.py`
  - 内存 repo、snapshot hydrate、FalkorDB hydrate 优先级。

- `src/lineage_graphrag/storage/falkordb_client.py`
  - 写入三类物理图 `raw/comm/repre`。

- `src/lineage_graphrag/impact/propagation.py`
  - 影响分析从 target 开始 BFS。

### 答不上会直接挂的代码问题

- 答不上 graph 里四层节点是什么：attribute level1、entity level2、keyword level3、community level4。
- 答不上 evidence chunks 如何和边关联：entity、transition、subgraph chunk id 通过 `evidence_refs` 挂到边上。
- 答不上 Path1 / Path2 分别召回什么。
- 答不上 Agent 和 noagent 的真实差异。
- 答不上本项目 FalkorDB 分区为什么有 `graph_raw / graph_comm / graph_repre`。
- 答不上影响分析为什么当前没有真正使用 `change_spec.source / relation_property_patch`。

## 4. 项目中的踩坑点

### 1. FalkorDB 不是主存储

- 问题背景：导入时同时保存内存、snapshot、FalkorDB。
- 原因：`GraphRepository` 的查询主路径是 `_graphs`，FalkorDB 只在写入和启动 hydrate 时使用。
- 解决思路：面试中要明确这是“内存主链路 + 外部图数据库镜像”的设计。
- 面试怎么讲：当前牺牲分布式一致性和大图查询能力，换来原型简单、测试可控、离线可跑。

### 2. 图数据库写入分区

- 问题背景：FalkorDB 写入被拆成 raw、comm、repre 三类物理图。
- 原因：不同层关系语义不同，raw 保存 entity/attribute/has/transitions，comm 保存 community/keyword，repre 保存代表关系。
- 解决思路：用 `_partition_graph_for_falkordb()` 按 label/relation 分流。
- 面试怎么讲：这是为可视化和语义隔离做的工程取舍，但查询时要 merge 回 NetworkX。

### 3. Agent 名字容易被追问

- 问题背景：`mode=agent` 不一定真的调用 LLM。
- 原因：LLM provider 默认是 stub，只有 OpenAI client 初始化成功时才跑 IRCoT 循环。
- 解决思路：先做确定性分解/检索，再有条件进入 LLM 迭代。
- 面试怎么讲：这是“LLM optional”的 Agent，不是强依赖 ReAct 工具调用框架；优势是稳定，弱点是 Agent 能力较轻。

### 4. what-if 语义和实现有偏差

- 问题背景：接口接收 `source/target/relation/relation_property_patch`。
- 原因：当前实现只从 `change_spec.target` 做 downstream BFS，没有模拟 patch，也没有校验 relation。
- 解决思路：面试时必须主动承认当前是“下游传播分析”，不是完整变更仿真。
- 面试怎么讲：可以说当前版本解决“这个节点之后影响谁”，还没解决“某条边属性变化后影响强度/规则如何变化”。

## 5. 合格标准

达到以下标准，就可以停止复习，去面试：

- 能在 3 分钟内讲清：`import -> parser -> normalizer -> builder -> repo/snapshot/FalkorDB -> query -> decomposer -> dual retrieval -> answer -> impact BFS`。
- 能画出或口述四层图结构：attribute、entity、keyword、community，以及 `has_attribute / has / transitions / member_of / represented_by` 的关系。
- 能用 `job_video_tag_enrich -> dwd_video_profile -> moderation_rule_engine` 这个 fixture 例子讲一次查询和一次影响分析。
- 能准确回答 Path1 和 Path2：Path1 是 node+relation，一跳扩展；Path2 是 triple+community，高层语义召回。
- 能解释 Agent 模式的边界：LLM 不可用时不会 IRCoT，只返回基于检索证据的 fallback 答案。
- 能主动说出 2 个局限：FalkorDB 不是主查询引擎；what-if 没有真正应用 patch。
- 面试官随机打开上述 6 个核心文件中的任意一个，能指出它在主链路里的位置。

结论：达到以上标准，这个项目可以用于后端 / Agent / GraphRAG 方向面试。

## 6. 当前项目的不足

### 明显不足

- 生产级不足：没有鉴权、并发写控制、异步任务队列、大图分页/增量构建、线上观测指标。
- RAG 质量不足：chunk ranker 只是 token overlap，缺少评测集、召回率指标、reranker、答案引用精确化。
- Agent 深度不足：当前 IRCoT 依赖文本约定抽取 `the new query is` / `so the answer is`，不是严谨 tool-calling agent。
- what-if 不完整：`relation_property_patch` 没有参与计算，影响传播也没有按关系类型过滤。
- 测试验证不足：仓库有测试，但当前本机环境没法运行；`pytest` 不存在，`python3` 是 3.7 且无 pytest。

### 最高优先级改进点

- 如果目标是“项目面试能过”：优先补强 what-if 语义解释和 Agent 边界说明。
- 如果目标是“高级后端系统面试”：优先补强持久化一致性、并发、异步构建、可观测性这几个生产化问题。
