services:
  falkordb:
    image: falkordb/falkordb:latest
    container_name: lineage-falkordb
    ports:
      - "6379:6379"
    volumes:
      - falkordb_data:/var/lib/falkordb/data

volumes:
  falkordb_data:

